"""
Train the GPT model on tiny Shakespeare.

Entry point for Phase 4+. Downloads the dataset if needed,
tokenizes it, creates train/val split, and runs training.
"""
from sml.tokenizer import Tokenizer
from sml.model import GPT
from sml.train import train


def main():
    raise NotImplementedError


if __name__ == '__main__':
    main()
